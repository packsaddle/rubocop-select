require 'spec_helper'

describe Rubocop::Select do
  it 'has a version number' do
    expect(Rubocop::Select::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
